import ContentSections from '@/components/content-sections';

// Import start-page specific styles
import '@/styles/content/start.css';

/**
 * Start Page (Homepage)
 * Displays all start page content sections:
 * 1. Hero Section (branding, CTAs)
 * 2. AKSEP Description & Pillars (Akronym)
 * 3. Program Highlights (Programm)
 * 4. News/Updates (Aktuell)
 * 5. Participation Options (Mitmachen)
 */
const StartPageContent = () => {
  return <ContentSections />;
};

export default StartPageContent;